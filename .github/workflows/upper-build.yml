name: upper-build 测试
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: zhangchen1
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
        run: go version

